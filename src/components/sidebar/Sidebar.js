import React from "react";
import classes from "../../styles/Sidebar.module.css";

const Sidebar = () => {
  return (
    <div className={classes.sidebar}>
      <h1 className={classes.sidebar__header}>Task App</h1>
    </div>
  );
};

export default Sidebar;
